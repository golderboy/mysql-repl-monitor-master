# R3.1.7a UI hotfix

## Fix
- Reports HTML page no longer crashes when the test harness injects a dummy connection without `cursor()`.
- UI report overview now falls back to an empty dataset instead of aborting the whole page.

## Scope
- `app/webapp.py`

## Expected result
- `GET /reports` returns HTML and JSON in test mode with mocked `list_report_exports`.
- Existing report pages still render when `build_report_dataset` is patched in tests.
