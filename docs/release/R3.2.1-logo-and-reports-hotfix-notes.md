R3.2.1 logo and reports hotfix

- Fix /reports HTML test failure when test harness injects DummyConn without cursor()
- Treat missing optional DB cursor path as fallback dataset instead of 500 for report overview cards
- Change logo references to url_for(static) with cache-busting query string
- Add graceful logo fallback text when moph.png cannot be loaded by browser
