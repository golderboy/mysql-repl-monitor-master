# R3.1.7b UI nav and reports fix

## Goal
Fix production UI navigation and report discoverability, and harden CSS/layout for browser testing before production.

## Included changes
- refresh `app/templates/base.html` so Reports is visible in main nav when role has `reports.view`
- add direct quick links to `/reports/admin` and `/reports/executive` in header toolbar
- refresh `app/templates/dashboard.html` to expose Trends / Admin report / Executive report / Advanced settings
- refresh `app/templates/access.html` to expose Reports and Audit links from the Access page
- refresh `app/templates/reports.html` so Daily/Admin/Executive report links are obvious at top of page
- harden CSS in `app/static/css/main.css` for form spacing and responsive layout
- no DB migration and no route/path change

## Validation done in build environment
- python syntax compile: passed
- Jinja template parse: passed

## Manual validation required on target host
- open `/reports`, `/reports/daily`, `/reports/admin`, `/reports/executive`
- confirm Reports button is visible in top nav for admin/operator roles
- restart web service after copying files so templates and static assets reload
